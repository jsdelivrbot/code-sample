import React from 'react'
import Fade from 'components/Fade/Fade'

const UnreadEmailCardTransition = (props) => {
  return (
    <Fade
      {...props}
    />
  )
}

UnreadEmailCardTransition.propTypes = {
}

export default UnreadEmailCardTransition
